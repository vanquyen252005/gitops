apiVersion: v1
kind: Service

metadata:
  name: gitops-demo

spec:
  selector:
    app: gitops-demo

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
